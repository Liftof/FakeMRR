{
  "name": "fakemrr",
  "version": "1.0.0",
  "description": "The database of unverified startup revenues",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
